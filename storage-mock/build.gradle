version = '3.0-SNAPSHOT'
description = 'mongoose - storage mock'

dependencies {
	compile project(':common')
}

jar {
	from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
	manifest {
		attributes 'Main-Class': 'com.emc.mongoose.Main'
	}
}