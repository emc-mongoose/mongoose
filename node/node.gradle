description = "mongoose-node"

dependencies {

	compile(
		project(":scenario"),
	)
}

jar {
	manifest {
		attributes (
			"Class-Path": classPathString(configurations.runtime),
			"Main-Class": "com.emc.mongoose.node.Main",
		)
	}
}

