description = "mongoose-node"

ext.isExt = false
ext.jarFileName = description
ext.mainClass = "com.emc.mongoose.node.Main"

dependencies {

	compile(
		project(":scenario"),
		"org.apache.logging.log4j:log4j-core:${depVersion.log4j}",
	)
}
