description = "mongoose-api-common"

dependencies {
	compile "org.apache.commons:commons-collections4:${depVersion.commonsCollections}"
	compile "commons-lang:commons-lang:${depVersion.commonsLang}"
}

jar {
	manifest {
		attributes (
			"Class-Path": setClassPath(configurations)
		)
	}
}
