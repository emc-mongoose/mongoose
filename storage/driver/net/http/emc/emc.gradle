description = "mongoose-storage-driver-net-http-emc"

dependencies {
	compile(
		"io.netty:netty-common:${depVersion.netty}",
	)
}

jar {
	manifest {
		attributes (
            "Class-Path": classPathString(configurations.runtime),
		)
	}
}
