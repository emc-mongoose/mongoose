description = 'mongoose-common'

dependencies {
	compile "org.apache.commons:commons-collections4:${depVersion.commonsCollections}"
}

jar {
	manifest {
		attributes (
			"Class-Path": setClassPath(configurations)
		)
	}
}
