description = "mongoose-scenario-step"

ext.isExt = false
ext.jarFileName = description

dependencies {

	compile(
		project(":api:metrics"),
		project(":api:model"),
		project(":ui"),
		"com.github.akurilov:java-concurrent:${depVersion.javaConcurrent}",
	)
}
